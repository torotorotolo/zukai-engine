__pycache__/
*.pyc

# 生成物はクラウドの成果物として受け取る。リポジトリには入れない
out/

# ref は事故検証で使うものだけ載せる（許可制。増やすときは下に1行足す）
# ⚠️ 健康解説chの参考画像（解剖図・表情図版・連続写真）は 2026-08-01 に削除済み
ref/*
!ref/CREDITS.md
!ref/aloha_left.jpg
!ref/aloha_normal.jpg
!ref/aloha_aftermath.jpg
!ref/ref_aloha_after.jpg
!ref/ref_aloha_fuselage.png
!ref/ref_aloha_route.jpg
!ref/b737_3view.png
!ref/ntsb_aar8903.pdf
!ref/ntsb_titan_MIR2536.pdf
# タイタン号（本番1本目）の素材。すべて NTSB 撮影＝PD。出所は ref/CREDITS.md に記録
!ref/titan_hull_edge.jpg
!ref/titan_hull_inner.jpg
!ref/titan_delam_ruler.jpg
# タイタニック船首（NOAA/IFE/URI・PD）。サムネの地に使う
!ref/titan_titanic_bow.jpg
!ref/titan_hull_pair.jpg
# NTSB報告書の図13〜18から取り出した標本写真（出所表記が無い＝NTSB撮影＝PD）
!ref/titan_ntsb14_endface.jpg
!ref/titan_ntsb15_endpiece.jpg
!ref/titan_ntsb16_wrinkle.jpg
!ref/titan_ntsb16_grind.jpg
!ref/titan_ntsb17_layers.jpg
!ref/titan_ntsb17_voids.jpg
# 取り出しの作業用
ref/_ntsb_raw/
# USCG が公開したタイタン号本体の写真（PD）
!ref/titan_rov_tailcone.jpg
!ref/titan_rov_aft.jpg
!ref/titan_cf_evidence.jpg

# ── 2026-08-02：PD限定をやめたので戻した報告書の図（`tools/extract_figs.py`）──
# 🔴 ここに1行足さないと **`git add -A` が黙って落とす**。r24 はそれで失敗した
#    （ローカルにはファイルがあるので机上検査は通り、写真を実際に開く build_jiko は
#     CI でしか走らないため、クラウドで初めて FileNotFoundError になる）。
# ⚠️ 出所は ref/CREDITS.md の台帳と1対1。PD以外も含む（カズヤくん判断・2026-08-01）。
!ref/titan_f18_delam.png
!ref/titan_f01_descend.jpg
!ref/titan_f04_lars.png
!ref/titan_f08_launch.png
!ref/titan_f09_parking.png
!ref/titan_f10_mishap.png
!ref/titan_f11_wreck.png

# 🔴 ナレーションの wav はリポジトリに入れない（1本34分で163MB）。
#    git は履歴を消せないので、作り直すたびに積み上がる。
#    **コミットするのは audio/opus/*.opus（22MB）**。クラウドは焼く前に
#    `python tools/audio_pack.py unpack` で wav に戻す（audio_mix.py は無変更）。
audio/*.wav
# 読みのドライラン結果と、通し確認用につないだ音声（どちらも作り直せる）
audio/kana_dry.txt
audio/_full.mp3
# USCG報告書は335ページ26MB。ヘッダー付きでいつでも落とせるので入れない
#   → 落とし方は Vault の引き継ぎノートに書いてある
ref/uscg_titan_mbi.pdf

# 🔴 既製BGM（DOVA-SYNDROME）は**作者が再配布を禁止**しているので置かない。
#    このリポジトリは public。Modal の保管庫から読む（tools/audio_mix.py 参照）
assets/

# 🔴🔴 YouTube の OAuth トークンと投稿メタ（2026-08-02）。**このリポジトリは public。**
#    token を1度でも push したら、その瞬間にチャンネルを他人へ渡したのと同じになる。
#    git は履歴を消せないので、消しても取り返せない。
#    → `tools/upload_jiko.py` は保存する前に `git check-ignore` で無視を実測し、
#      無視されていなければ**書かずに落ちる**（この行を消すと落ちるようにしてある）。
config/
