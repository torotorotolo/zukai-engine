__pycache__/
*.pyc

# 生成物はクラウドの成果物として受け取る。リポジトリには入れない
out/

# ref は事故検証で使うものだけ載せる（許可制。増やすときは下に1行足す）
# ⚠️ 健康解説chの参考画像（解剖図・表情図版・連続写真）は 2026-08-01 に削除済み
ref/*
!ref/CREDITS.md
!ref/aloha_left.jpg
!ref/aloha_normal.jpg
!ref/aloha_aftermath.jpg
!ref/ref_aloha_after.jpg
!ref/ref_aloha_fuselage.png
!ref/ref_aloha_route.jpg
!ref/b737_3view.png
!ref/ntsb_aar8903.pdf
!ref/ntsb_titan_MIR2536.pdf
# タイタン号（本番1本目）の素材。すべて NTSB 撮影＝PD。出所は ref/CREDITS.md に記録
!ref/titan_hull_edge.jpg
!ref/titan_hull_inner.jpg
!ref/titan_delam_ruler.jpg
# タイタニック船首（NOAA/IFE/URI・PD）。サムネの地に使う
!ref/titan_titanic_bow.jpg
!ref/titan_hull_pair.jpg
# NTSB報告書の図13〜18から取り出した標本写真（出所表記が無い＝NTSB撮影＝PD）
!ref/titan_ntsb14_endface.jpg
!ref/titan_ntsb15_endpiece.jpg
!ref/titan_ntsb16_wrinkle.jpg
!ref/titan_ntsb16_grind.jpg
!ref/titan_ntsb17_layers.jpg
!ref/titan_ntsb17_voids.jpg
# 取り出しの作業用
ref/_ntsb_raw/
# USCG が公開したタイタン号本体の写真（PD）
!ref/titan_rov_tailcone.jpg
!ref/titan_rov_aft.jpg
!ref/titan_cf_evidence.jpg

# ── 2026-08-02：PD限定をやめたので戻した報告書の図（`tools/extract_figs.py`）──
# 🔴 ここに1行足さないと **`git add -A` が黙って落とす**。r24 はそれで失敗した
#    （ローカルにはファイルがあるので机上検査は通り、写真を実際に開く build_jiko は
#     CI でしか走らないため、クラウドで初めて FileNotFoundError になる）。
# ⚠️ 出所は ref/CREDITS.md の台帳と1対1。PD以外も含む（カズヤくん判断・2026-08-01）。
!ref/titan_f18_delam.png
!ref/titan_f01_descend.jpg
!ref/titan_f04_lars.png
!ref/titan_f08_launch.png
!ref/titan_f09_parking.png
!ref/titan_f10_mishap.png
!ref/titan_f11_wreck.png

# ══ 2本目：日本航空123便（62-2 JA8119）══════════════════════
# 🔴 **題材ごとのフォルダに切り替えた**（1枚ずつ許可を足すのは、写真を差し替えるたびに
#    書き忘れが起きるため。r24 は 40分焼いた最後に FileNotFoundError で落ちた）。
# 中身は運輸安全委員会の事故調査報告書から機械的に切り出した 168 枚
#   p001〜p124 … 写真‐1〜124 ／ f001〜f038 … 付図‐1〜38
#   a1p001,002 … 別添1 写真‐1,2 ／ a1f001〜004 … 別添1 付図‐1〜4
# 取り出しは `python tools/extract_photos.py`（出どころと根拠は ref/CREDITS.md）。
# 権利は **公共データ利用規約 PDL1.0**（CC BY 4.0 互換。複製・公衆送信・翻案を許諾）。
!ref/ja123/

# ══ 3本目：スレッシャー号（USS Thresher SSN-593 / 1963-04-10）══════════
# 中身は3つ。出どころと根拠は ref/CREDITS.md の台帳と1対1。
#   thr_t1〜t41.jpg … NARA 289-T アルバム41ページから写真部分を切り出したもの
#   thr_fig*.png    … 査問会記録 第9・10次公開から取り出した海図・データ表
#   thr_cm_*.jpg    … Wikimedia Commons の PD 写真（CC BY-SA は入れない＝継承を避ける）
# 権利＝米海軍の職務著作でパブリックドメイン（NARA "Works of the U.S. Government"）
!ref/thresher/
# 🔴 原本（アルバム5860px×41点＝350MB）と査問会PDF（128MB）は**入れない**。
#    このリポジトリは public で、git は履歴を消せない。原本は下の作業用に置く。
ref/_thresher_raw/

# ══ 4本目：サーフサイド（Champlain Towers South / 2021-06-24）══════════
# 中身は3種。出どころと根拠は ref/CREDITS.md の台帳と1対1。すべて NIST の職務著作＝PD。
#   tf_pNNN_*.jpg … 技術的知見（2026-06-22・77分の4K動画）のスライドを1コマ抜いて切り出したもの
#                    ⚠️ `©2021 Used with permission` の写真（p45/p103/p119/p147、p50・p52 の右上）と
#                       Google の画像（p184）、管財人の写真と原図（p57・p58）は**切り落としてある**
#   fb_<cid>.jpg  … 記録映像（B-Roll）のひかえの静止画（動画のコマが取れないときだけ出る）
#   ss_*.jpg      … 記録映像からわざと抜いた1コマ（空撮など）
!ref/surfside/
# 🔴 4K の原コマ（PNG・1枚4〜9MB）は analytics/ に置く。ここには入れない。

# 🔴 ナレーションの wav はリポジトリに入れない（1本34分で163MB）。
#    git は履歴を消せないので、作り直すたびに積み上がる。
#    **コミットするのは audio/opus/*.opus（22MB）**。クラウドは焼く前に
#    `python tools/audio_pack.py unpack` で wav に戻す（audio_mix.py は無変更）。
audio/*.wav
# 読みのドライラン結果と、通し確認用につないだ音声（どちらも作り直せる）
audio/kana_dry.txt
audio/_full.mp3
# 🔴 ElevenLabs の行ごとの合成キャッシュ（pcm。1本で約100MB・作り直せる）。
#    検査の記録 audio/el_qa/*.tsv は小さいので入れる（検査台帳の正本）
audio/el_cache/
# USCG報告書は335ページ26MB。ヘッダー付きでいつでも落とせるので入れない
#   → 落とし方は Vault の引き継ぎノートに書いてある
ref/uscg_titan_mbi.pdf

# 🔴 既製BGM（DOVA-SYNDROME）は**作者が再配布を禁止**しているので置かない。
#    このリポジトリは public。Modal の保管庫から読む（tools/audio_mix.py 参照）
assets/

# 🔴🔴 YouTube の OAuth トークンと投稿メタ（2026-08-02）。**このリポジトリは public。**
#    token を1度でも push したら、その瞬間にチャンネルを他人へ渡したのと同じになる。
#    git は履歴を消せないので、消しても取り返せない。
#    → `tools/upload_jiko.py` は保存する前に `git check-ignore` で無視を実測し、
#      無視されていなければ**書かずに落ちる**（この行を消すと落ちるようにしてある）。
config/

# 🔴 アナリティクスの取得結果は public リポジトリに置かない（2026-08-07）。
#    チャンネルの内部数字（再生・維持・CTR・Studio の CSV）が入る。
analytics/

# 5本目 SL-1（2026-09-07）
!ref/sl1/
!ref/sl1/shots.json
!ref/sl1/materials.json
ref/sl1/*.pdf

# ── 2026-09-07（④台本）：一次資料の**本文**だけリポに入れる ──
# 🔴 PDF（IDO-19302 17MB／IDO-19311 29MB／IDO-19313 33MB／ANL-6692 5.6MB／HAER 7.4MB）は
#    上の `ref/sl1/*.pdf` で入れない。取り直しの URL は ref/CREDITS.md に実測つきで書いた。
# ⚠️ ただし**本文が手元に無いと原文照合ができない**（OSTI が落ちたら止まる）ので、
#    抜き出した全文テキストだけは入れる。1.1MB。
!ref/sl1/ido_all.txt
!ref/sl1/IDO-19311_all.txt
!ref/sl1/IDO-19313_all.txt
!ref/sl1/ANL-6692_all.txt
!ref/sl1/HAER_ID-33-D_all.txt
# ページ単位に割ったものと切り出した PNG は入れない（全文と PDF から作り直せる）
ref/sl1/*_text/
ref/sl1/_crops/

# 🔴 題材の帳簿（2026-09-08・カズヤくん指示「毎回1から調査するのはトークンの無駄」）。
#    毎回の①題材チャットで測り直さないための蓄積。中身は**他局の公開データと素材の実測値だけ**で、
#    このchの内部数字（再生・維持・CTR）は1件も入らない（それは analytics/ ＝ git 管理外のまま）。
!ref/themes/
!ref/themes/README.md
!ref/themes/ledger.json
!ref/themes/pool.json

# ── 6本目 キー橋（2026-09-08 ②素材）────────────────────────
# ⚠️ 9行目の `ref/*` が ref/ を丸ごと無視する造り（許可制）。**フォルダから明示的に開ける。**
!ref/keybridge/
# NTSB の報告書 PDF は 12.2MB あるのでリポに入れない。取り直しは ref/CREDITS.md §キー橋 の URL 1行。
ref/keybridge/*.pdf
# 入れるもの＝抽出した本文（④の原文照合がこれで済む）・素材の台帳・照合ずみの数字
!ref/keybridge/ntsb_MIR2540.txt
!ref/keybridge/ntsb_MIR2510.txt
!ref/keybridge/FACTS.md
!ref/keybridge/SHOTS_INDEX.md
!ref/keybridge/clips.json
!ref/keybridge/shots.json
# 話速の実測（el_speed_probe）の振り直し記録は作業の跡なので入れない
audio/el_qa/probe*_el_retakes.tsv

# ── 7本目 9.11（2026-09-13 ②素材＋③尺）──────────────────
# ⚠️ 9行目の `ref/*` が ref/ を丸ごと無視する造り（許可制）。**フォルダから明示的に開ける。**
!ref/ep7/
# 入れるもの＝素材の台帳（ffprobe の実測から機械で書き出したもの）と章立て
!ref/ep7/clips.json
!ref/ep7/kousei.md
# ⑤で `tools/shots.py` が作るショットの境目の表（まだ無い）
!ref/ep7/shots.json
!ref/ep7/SHOTS_INDEX.md
# 一次資料の PDF は入れない（NTSB 4本で 21.3MB）。取り直しは ref/CREDITS.md §9.11 から
ref/ep7/*.pdf

# ── 8本目 コロンビア号（2026-09-14 ②素材＋③尺）────────────────
# ⚠️ 9行目の `ref/*` が ref/ を丸ごと無視する造り（許可制）。**フォルダから明示的に開ける。**
!ref/ep8/
# 入れるもの＝章立て・素材の台帳・素材の網（どれも機械の実測から書いたテキスト）
!ref/ep8/kousei.md
!ref/ep8/materials.md
!ref/ep8/slots_nasa.json
# ⑤a で確定した「概要欄に載せる用語」の正本（⑥がそのまま貼る）
!ref/ep8/yougo.md
# 動く映像の出どころと権利（Commons の extmetadata をそのまま保存したもの）
!ref/ep8/vid/
!ref/ep8/vid/sources.json
# 入れないもの＝落とした実体
#   CAIB Vol.I（10.3MB・入口は ref/ep8/materials.md §4 にある）
ref/ep8/*.pdf
#   動く映像9本（918MB）。取り直しは vid/sources.json の url から
ref/ep8/vid/*.webm
ref/ep8/vid/*.mp4

# ── 9本目 テネリフェ空港衝突（2026-09-16 ②素材＋③尺）──────────────
# ⚠️ 上の `ref/*` が ref/ を丸ごと無視する造り（許可制）。**フォルダから明示的に開ける。**
!ref/ep9/
# 入れるもの＝章立て・素材の台帳・素材の網（どれも機械の実測から書いたテキスト）
!ref/ep9/kousei.md
!ref/ep9/materials.md
!ref/ep9/slots_commons.json
# 入れないもの＝一次資料の実体（PDF 11本・14.6MB）。
#   取り直す入口は ref/ep9/materials.md §4 に全部 URL で書いてある
ref/ep9/*.pdf

# ── 10本目 三豊百貨店崩壊（2026-09-19 ①題材の実測）──────────────
!ref/ep10/
# 入れるもの＝素材の道・一次資料の調査メモ（どれもテキスト）
!ref/ep10/materials.md
!ref/ep10/primary_sources.md
!ref/ep10/ko_public_sources.md
# ②③（2026-09-20）で足したもの＝章立てと尺・欄ごとの素材（機械が出した正本）
!ref/ep10/kousei.md
!ref/ep10/slots_commons.json
# 入れないもの＝一次資料と素材の実体（国会会議録・判決・白書の頁画像・Commons の640px版・公共ヌリの原本）
#   取り直す入口＝materials.md §4（一次資料）／§8-2（白書）／tools/ep10_commons.py（Commons）
ref/ep10/src/*
# ②b（2026-09-20）で例外にした2件＝**目で見た記録は取り直せない**ので git に残す（どちらも小さいテキスト）
!ref/ep10/src/seen.tsv
!ref/ep10/src/order.json

# ── 11本目 チャレンジャー号 STS-51-L（2026-09-21 ①題材＋②素材）──────────
# ⚠️ 上の `ref/*` が ref/ を丸ごと無視する造り（許可制）。**フォルダから明示的に開ける。**
!ref/ep11/
# 入れるもの＝素材の台帳・欄の定義・実測の束（どれも機械の実測から書いたテキスト）
!ref/ep11/materials.md
!ref/ep11/slots_nasa.json
# NASA 画像庫 298点の実測（原本の画素を1点ずつ Range で測った値が入っている＝測り直しが高い）
!ref/ep11/nasa_ep11.json
# 一次資料＝ロジャース委員会報告書 第I巻の本文テキスト（約700KB）。④と④' が原文照合に使う
!ref/ep11/src/
!ref/ep11/src/*.txt
# ③尺の正本（章立て・尺の根拠・動く映像の判定）と、③④で使った測りの道具3本
!ref/ep11/kousei.md
!ref/ep11/slot_check.py
!ref/ep11/dur_probe.py
!ref/ep11/dur_test.py
# 入れないもの＝落とした実体と、探索の途中結果
#   生の HTML（約1MB）・archive.org の探索結果・NASA 画像庫の動画側の探索結果。
#   取り直す入口は ref/ep11/materials.md §1・§3・§5 に URL とコマンドで書いてある
ref/ep11/src/*.htm
ref/ep11/src/*.html
ref/ep11/ia_ep11.json
ref/ep11/nasa_ep11_video.json
# 🔴 動く映像の実体（合計 約440MB）。③で中身を確かめた結果は ref/ep11/kousei.md §4 にある。
#   取り直しは kousei.md §4-2 の archive.org の項目名から。⚠️ ⑤bで使うのは 640×480 の .mpg
ref/ep11/vid/

# ⑤c-2（2026-09-20）原寸の切り出し。**焼けた絵から何度でも作り直せる**ので git に載せない
# （23枚で 6.3MB。公開リポジトリなので派生画像は置かない）。作り直す＝qa_out/ep10_crop.py
qa_out/crop/
ref/ep11/vid/clips/
ref/ep11/sheets/

# ── 12本目 キャッスル・ブラボー水爆実験（2026-09-22 ②素材＋③尺）──────────
# ⚠️ 上の `ref/*` が ref/ を丸ごと無視する造り（許可制）。**フォルダから明示的に開ける。**
!ref/ep12/
# 入れるもの＝台帳2枚（②の素材・③の尺と章立て）
!ref/ep12/materials.md
!ref/ep12/kousei.md
# 実測の束（取り直しが高い／目で見た記録は取り直せない）
#  commons_ep12.json … Commons 629点の生データ。⚠️ Commons は 429 で止めるので取り直しに20分かかる
!ref/ep12/commons_ep12.json
!ref/ep12/commons_hidden.json
!ref/ep12/commons_ep12.txt
!ref/ep12/commons24.json
#  NARA RG678 の Project 22（51点）。原本の画素は 1点ずつ Range で測った値
!ref/ep12/nara_castle_detonation.json
!ref/ep12/nara_castle_testact.json
!ref/ep12/nara_sizes.json
#  動く映像の「実効の枠」の実測（器の札が嘘をつくので、測り直すまで正本はこれ）
!ref/ep12/box_cr.json
!ref/ep12/box_doe.json
# 一次資料の本文テキスト（DNA 6035F 538頁／WT-923 Project 4.1 96頁／DASA 1251 Vol.II 351頁）
!ref/ep12/src/
!ref/ep12/src/*.txt
# ②③で書いた道具（次の回でも使う）
!ref/ep12/probe_commons.py
!ref/ep12/probe_nara.py
!ref/ep12/probe_nara_series.py
!ref/ep12/measure_nara_sizes.py
!ref/ep12/net4.py
!ref/ep12/fetch_and_dedup.py
!ref/ep12/extract_pdf.py
!ref/ep12/measure_effective_res.py
!ref/ep12/measure_watermark_box.py
!ref/ep12/scan_burned_text.py
!ref/ep12/dur_ep12.py
# 🔴 失敗した物差しも残す（同じ間違いを繰り返さないため）
#   find_watermark.py … コマ間の分散で透かしを探す版。**動く海の上の透かしを見落とした**
!ref/ep12/find_watermark.py
#   measure_bravo4k.py … 縮小→拡大の差で実効解像度を測る版。**陽性対照が0に張り付かず使えなかった**
!ref/ep12/measure_bravo4k.py
# 入れないもの＝落とした実体と探索の途中結果
#   PDF 3本（合計 約37MB）。取り直す入口は materials.md §5 に URL で書いてある
ref/ep12/src/*.pdf
#   Commons の24点の実体とシート（合計 約57MB）。fetch_and_dedup.py で作り直せる
ref/ep12/img/
#   NARA の全文検索の生データ（640KB）。probe_nara.py で取り直せる
ref/ep12/nara_ep12.json
