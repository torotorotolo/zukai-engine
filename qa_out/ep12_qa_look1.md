# 12本目（キャッスル・ブラボー）⑤c 検品【見る】台帳 ── シート全数

**作った日**：2026-09-23　**版**：`e7c1725`（`git pull --rebase toro main` で Already up to date・作業ツリーは空）
**カット数**：191（図103／写真映像88＝46.1%）　**シート**：2列×3行・1枚6カット・1コマ640px

---

## 0. 🔴🔴 終わりの決まり（⑤c に入る前に先に書く）

正本＝記憶 `feedback-jiko-qa-must-have-an-end`・ルール統合版 §5c-2。**粗が見つかる限り続ける形にはしない。**

| | |
|---|---|
| **残りのチャット** | ⑤c シート（**このチャット＝10本目**：焼く＋シートで全数）／⑤c 原寸（11本目：疑いだけ原寸）／⑤c'（12本目：直す＋焼き直し）／⑥（13本目：本レンダ〜公開）＝ **あと3チャット**（予算 11〜14 の中・余り1） |
| **焼き直し** | **r02・r03 まで**。r03 で残ったものは次回の注意へ |
| **最後の検品の範囲** | **直したカットだけ**。ほかは見ない |
| **合格の基準** | ① 門番が全部 ✓　② 直したカットを原寸で確認　③ 直しが絵を悪くしたら**直し直さず、見終わった版の SPEC へ戻す**（戻したカットだけ焼いて確かめる）　④ それ以外の粗は**直さず「次回の注意」へ** |
| **このチャットでやらないこと** | **直さない・焼き直さない**。所見は🔴と「式で直せる⚠️」だけ・1行1粗・決着欄つき。640px で決めない3型（図の比例・小さな点・札の切れ）は🔴を付けず「原寸で測る」 |
| **検品を広げる提案** | 名簿を足す・全数をもう1周などは「試写までのチャット数を増やす提案」として数字を添えて出す。黙って計画に入れない |

---

## 1. 門番（このチャットで自分で回しました。前のチャットの出力は引き写していません）

| 門番 | 結果 | 備考 |
|---|---|---|
| `tools/qa_all.py` | **exit 0・18本すべて ✓** | cuts・chapters・layout・wording・echo・dup・box・color・palette・drift・slide・blank・wrap・yomi_num・slots・footage・credits・photomask |
| `tools/check_drift.py` | **exit 0 ✓** | **17カット・照合57件・食い違い0件** |
| 写真映像の比 | **88/191＝46.1%** | 写真46・頁23・動く映像のひかえ（`fb_`）19（下の §2-3） |
| コミットの空き | 6,334MB | 下限 300MB に対して十分 |

⚠️ `photomask` は ✓ だが「`NEEDS_MASK` が空＝0件を調べて合格にしていないか」の🔴行を出す。
　 ⑤b-4 は私人の顔を `ss.TRIM`（切り落とし）で処理したので名簿が空なのは筋が通る。
　 ＝**シートでは写真の人物の顔（私人かどうか）を観点に入れる**。

---

## 2. 焼く前に機械で当てた当たり（鉄則2＝目視は最後の網）

### 2-1. 同じ写真の使い回し ── md5 ＋ SIFT の総当たり（使い捨て・scratchpad `twins_ep12.py`）

11本目 §4-3 のとおり `check_twin` は同じ1枚を捕まえないので、**使っている画像 88欄（異なるファイル85点）を総当たり**した。

| 物差し | 結果 |
|---|---|
| md5 が同じファイルを2カット以上 | **2組・どちらも意図どおり**：`c113`＝`c218`（`dev_shrimp_men`・副題も同文＝§5b-19）／`pg114` を `c404`（本文）と `c814`・`c815`（第16図） |
| SIFT の RANSAC 内点（別ファイルどうし 3,570組） | **最大 170**。分布 <15：3,366／15〜39：183／40〜99：18／≥100：3 |
| 陽性対照（同じ写真を7割に切る＋0.8倍＋JPEG再圧縮） | **内点 226〜504**（`dev_shrimp_men` 451・`fo_pig_lift` 504・`rongerik_station` 226・`pg230` 232）＝物差しは生きている |

→ **別ファイルに同じ写真がまぎれている組は無い。**

⚠️ **当たり**：`pg230`（c517）⇔`pg232`（c721）内点 **170**・`pg214`（c418）⇔`pg230` **146**・`pg214`⇔`pg232` **101**。
　 `pg230` の双子（232）に近い＝**同じ下地の地図の図が3回出る**可能性。シートで「別の図に見えるか」を見る。

⚠️ `qa_all` に画素の双子の門番は**まだ載っていない**（11本目 §4-3 の勧めが未実施）。⑤c' か次の回の道具で足す＝§5c-5。

### 2-2. ひかえの静止画（`fb_`）を使うカット ── §5c-9「先に全部見る」

| | カット |
|---|---|
| 動く映像（`footage.USE`）11欄＝取れれば動画・取れなければ `fb_` の静止画に落ちる | c101 c102 c110 c324 c501 c502 c506 c522 c606 c607 c608 |
| **止め絵として `fb_` をそのまま出す 8欄** | **c114 c313 c318 c422 c503 c504 c512 c914** |

---

## 3. 焼き（Actions）

（焼き上がりのあとに書く）

---

## 4. シートで見た記録

（シートを見ながら書く）
