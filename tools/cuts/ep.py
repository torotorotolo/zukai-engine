# -*- coding: utf-8 -*-
"""締め（ep）＋共通の末尾 `ed01`。12本目（キャッスル・ブラボー）。

■ 12本目には ep のカットが無い（台本第2版は第9章 c920 で終わる）。
  ここに残すのは**共通エンディング `ed01` だけ**。

⚠️ **⑤b-2 で空にしました**（2026-09-23）。11本目の中身は `git show 61039d2:tools/cuts/ep.py`。
🔴 **`ed01`（共通エンディング）は題材依存ではない**ので残す。**画は章ファイルに要る**
   ＝`check_cuts` は音の側（191カット）を正として数えるので、書かないと「画が無いカット」で鳴る。
   並び＝高評価（左）／アイコン（中央・少し大きく）／登録（右）（11本目から・`titan_fig.ending()`）
   → 記憶 [[project-jiko-common-ending]]／[[project-jiko-rules-index]] §0b
"""

SPEC = {

    # ── 共通エンディング（全回で同じ。2026-09-21 新設・2026-09-22 に並べ替え）──────
    # 🔴 構想＝2026-09-19 カズヤくん決定（記憶 project-jiko-common-ending）:
    #    暗転なし／高評価・アイコン・登録を**同じ1枚**に／グッドは押されて色が付く／
    #    登録は押されて「登録済み」に変わる／**文字は極力使わない**。
    # ⚠️ 見出しは全カットに付いている（型の決まり）ので、ここだけ無しにはできない。
    #    ⚠️ 見出しを「ご視聴ありがとうございました」にすると `check_echo` が
    #    「字幕の1文と句点を除いて丸ごと同一」で落ちる（＝画面に出した言葉を字幕にも出している）。
    #    礼はナレーションが言うので、見出しは**画面が何かを言うだけ**の一語にする。
    # ⚠️ 「この動画」は楽屋の言葉（B型）＝**画面には出さない**（ナレーションでは言う）。
    "ed01": dict(
        t="おわり",
        fig=("ending", dict()),
    ),

}
