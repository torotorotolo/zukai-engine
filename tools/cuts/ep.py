# -*- coding: utf-8 -*-
"""第10章「何が変わったか」 ca01–ca12（12カット）と、締め ep01–ep06（6カット）。

■ 🔴 なぜ第10章がこのファイルに入っているか
  `cuts/__init__.py` の `CHAPTER_FILES` は `pr, c1…c9, ep` の11本で、
  **6本目は冒頭＋10章＋締めなので章ファイルが1本足りない。**
  カットIDの頭で章を分けている道具（`dump_script.py` は `ca` を章として扱う）とは別に、
  `cuts.SPEC` は**平らな辞書**なので、どのファイルに書いても画は同じところに出る。
  → ⑤b-3 では `CHAPTER_FILES` を触らず、**第10章と締めを1本にまとめた**。
  ⚠️ 章を増やしたくなったら `cuts/ca.py` を作って `CHAPTER_FILES` に足す
     （そのときは `check_cuts.py` のカット数が 216 のまま変わらないことを見る）。

■ 第10章が追う物証（1つだけ）
  **勧告の宛先**（報告書 p.148〜p.151・p.179・p.202〜p.206）。誰に何をせよ、だけを並べる。

■ 締めが追うもの
  **28秒**（p.47）と、**同じ時刻に掛からなかった電話**（p.177）。

■ 🔴 台本 §4 の「画」から替えた欄が2つ（理由つき）

  | カット | 台本 | 実装 | なぜ |
  |---|---|---|---|
  | `ca02` | 図 p113 | **図 p125** | p113 は焼いていないページ。ワゴ社への勧告は「帯の位置が**線の差し込みを妨げうる**」なので、台帳のうち中身が合うのは p125 図60「端子台に差し込まれた線の深さ」。⚠️ pr06 と同じページなので**寄りと注記を変えてある** |
  | `ca10` | breakdown | **panel** | 台本 §9-2b：「20の道路管理者」は**勧告の宛先をこちらが数えた値**で、報告書の本文には無い。**画面には出さない**と台本が決めている。breakdown は total を必ず画面に出すので使えない |

  ⚠️ `ca05` は **c721 と同じ図 p146**（口の面に載っているだけの先端）。
     第7章では「触れているだけでも電気は通る」、ここでは「接する面が小さい＝温度が上がる」。
     **意図した反復**で、寄り（side）と注記を変えてある。

■ 🔴 `absent` の見せ方を散らした（README §2）
  ca06 `ledger` ／ ca08 `pair` ／ ep04 `single`。
  ⚠️ ca08 の「有る」側＝**船の乗員と水先人には沿岸警備隊への無線がある**。

■ 実写の欄（⑤b-4 で動画・⑤b-5 で残りを決め終えた）
    ca03 → `ss.page(147)`（Figure 65 正しい帯の付け方との比較）
    ca09 → `ss.page(173)`（Figure 71 点滅する赤い警告灯＝勧告が求めた装置そのもの）
    ep06 → `ss.PRE_2019`（2019年の全景。c101 の絵に戻って閉じる）
"""
import jiko_style as J
import cuts.ss as ss

SPEC = {

    # ══════════════════════════════════════════════════════
    #  第10章　何が変わったか（ca01–ca12）
    # ══════════════════════════════════════════════════════

    # ── ca01 勧告の宛先 ───────────────────────────────────
    # 実写 NTSB空撮（崩落した中央径間）
    "ca01": dict(
        # ⚠️ ⑤b-6：見出しは字幕の尻の切り取り、注記2本は6つの宛先の丸写しだった
        #    （check_echo 3件）。6者の**名前はナレーションが読み上げる**ので、
        #    画面は名前でなく**2つの側に分かれていること**と、その数を持つ。
        t="宛先は、2つの側に分かれる",
        # 🔴 2026-09-10（⑤c' 3巡目・§V-19）：副題が**中身の時点**（2025年の勧告）を
        #    名乗るのに、絵は**崩落の直後**（ダリと落ちたトラス）で1年ずれていた。
        #    ⚠️ 素材は `fb_*.jpg` なので門番は1件も鳴らない。→ 副題は**絵が何か**を言う。
        s="崩落の直後　航路に残ったダリ",
        photo=ss.fb("ca01"), bias=0.5, side="right", ann_y=340,
        # 🔴 同・§V-29：注記が「国と制度の側／3者」「船と部品の側／3者」だけで
        #    **画面に具体名も中身も1つも出ない**のに、字幕は6者を全部読み上げる
        #    ＝画面と字幕の情報量が食い違っていた。
        #    ⚠️ 名前は字幕が言うので画面へは出さない（⑤b-6 で check_echo が3件鳴った）。
        #    → **問いのほうに「どういう側か」を入れる**。名前を写さずに中身が増える。
        # 🔴 2026-09-10（⑤c' 4巡目・台帳 §Y-5）：答えが「3者」「3者」で**2段とも同じ**。
        #    画面から得られる新しいことが「3と3」だけになっていた。
        #    ⚠️ 名前は出せない（上のとおり check_echo が3件鳴る）ので、
        #      **合計の段を足して、2つの「3者」が内訳だと読めるようにする**。
        #      ナレーションが6者を読み上げるので、耳と画面が同じ数で合う。
        ann=[dict(t="規制と基準をつくる側", v="3者",
                  vc=J.INST, vs=88),
             dict(t="船をつくる側と、動かす側", v="3者", vc=J.LINE,
                  vs=88),
             dict(t="あわせて", v="6者", vc=J.AMBER, vs=72)],
    ),

    # ── ca02 ワゴ社へ ─────────────────────────────────────
    # 図 p125（端子台に差し込まれた線の深さ）。⚠️ pr06 と同じページ。**寄りと注記を変える。**
    "ca02": dict(
        t="説明書に、書き加えること",
        s="端子台を作っている会社への勧告",
        photo=ss.page(125), **ss.kind(ss.page(125)), side="left", ann_y=330,
        ann=[dict(t="宛先", v="ワゴ社", vc=J.INST, vs=88),
             # ⚠️ ⑤b-6：注記が字幕に丸ごと含まれていた
             dict(t="書き加えること",
                  d="帯が差し込みを妨げうる", dc=J.ALERT,
                  ds=28)],
    ),

    # ── ca03 造船所へ ─────────────────────────────────────
    # 図版 p147（Figure 65「正しい帯の付け方と、Wire 1 の付け方の比べもの」）。⑤b-5 で決めた。
    # 🔴 勧告の中身が「**帯の付け方**を作業手順に入れること」なので、この図版が主題そのもの。
    #    ⚠️ c725（帯が、線を奥まで入れさせなかった）でも使う。ここが2回目で、約8分あく。
    "ca03": dict(
        t="作業手順に、入れること",
        s="造船所への勧告",
        photo=ss.page(147), **ss.kind(ss.page(147)), side="right", ann_y=336,
        # 🔴 2026-09-10（⑤c' 4巡目・台帳 §Y-5）：ここは2つ直した。
        #    ① 段1の答え「帯の付け方」と段3の答え「差し込みを妨げない**帯の付け方**」で
        #       **1枚に同じ語が2回**出ていた → 段3は「ねらい」だけを言う形にする。
        #    ② 答えの級数が 80／56／28 の**3種**に割れていた。真因は段2の答えが
        #       9字と長くて `fit` が縮めること → 見出しが「作業手順」を言っているので
        #       段2は行き先だけにする（「電気部門」4字）。80／80／28 になる。
        ann=[dict(t="入れるもの", v="帯の付け方", vc=J.DOC, vs=80),
             dict(t="どこへ", v="電気部門", vc=J.INST, vs=80),
             # 🔴 2026-09-10（⑤c' 3巡目・§V-17）：答えが「…ないように」＝**動詞句**だった。
             dict(t="ねらい", d="差し込みを妨げないこと", dc=J.OK,
                  ds=28)],
    ),

    # ── ca04 運航会社へ ───────────────────────────────────
    # 図 p149（赤外線の熱画像による点検）
    "ca04": dict(
        # ⚠️ ⑤b-6：副題は字幕の1文の頭、段の値は字幕に丸ごと含まれていた（check_echo 2件）
        t="温度の差で、見つける",
        s="熱で見る点検",
        photo=ss.page(149), **ss.kind(ss.page(149)), side="right", ann_y=330,
        ann=[dict(t="宛先", v="運航会社", vc=J.INST, vs=88),
             dict(t="求めたこと", v="赤外線熱画像の点検", vc=J.DOC, vs=48)],
    ),

    # ── ca05 なぜ温度が上がるか ───────────────────────────
    # 図 p146（口の面に載っているだけの状態）。⚠️ c721 と同じページ。**寄りと注記を変える。**
    "ca05": dict(
        t="接する面が、小さかった",
        s="口の面に載ったままの先端",
        photo=ss.page(146), **ss.kind(ss.page(146)), side="left", ann_y=336,
        # ⚠️ ⑤b-6：注記が字幕に丸ごと含まれていた
        ann=[dict(t="あの線の状態", d="差し込み不足のまま接触",
                  dc=J.ALERT, ds=30),
             dict(t="そのため", v="温度が上がっていた", vc=J.AMBER, vs=56)],
    ),

    # ── ca06 義務づける決まりが無い ───────────────────────
    "ca06": dict(
        t="義務づける決まりは、無い",
        s="熱画像の点検の扱い",
        fig=("absent", dict(
            mode="ledger",
            items=[dict(t="船級協会", d="認めている", ok=True, c=J.INST),
                   dict(t="業界での使われ方", d="広く使われている", ok=True,
                        c=J.INK_W),
                   dict(t="使うことの義務", d="どこにも無い", ok=False,
                        c=J.ALERT)],
            note="報告書 p.151")),
    ),

    # ── ca07 規格を作る団体へ ─────────────────────────────
    # 🔴 「無い線」を描かない＝橋の上で働く人へ入る線が1本も無い（pr11・c410・c711 と同じ手）。
    "ca07": dict(
        # ⚠️ ⑤b-6：見出しは字幕の尻の切り取り、真ん中の節は 79% 一致だった（check_echo 2件）
        t="足りないのは、規格のほう",
        s="橋の上の作業員のための勧告",
        fig=("people", dict(
            # 🔴 2026-09-09（⑤c'・型③）：左端 x=0.06 で箱の幅が下限 300px に
            #    張りついていた（c506 と同じ形。理由は `cuts/c5.py` の c506 に書いた）。
            nodes=[dict(x=0.13, y=0.42, t="NTSB", d="勧告", c=J.INST,
                        kind="org"),
                   # 🔴 2026-09-10（⑤c' 3巡目・§V-8-1）：級数が 79/27/35 と **52px** 割れ、
                   #    このカットの主題（「足りないのは、規格のほう」）である真ん中が最小だった。
                   #    箱は 491px 取れているので純粋に字数の差。→ 長い題を短くする。
                   # 🔴 2026-09-10（⑤c' 4巡目・台帳 §Y-2）：級数の割れが**まだ残っていた**
                   #    （NTSB ink 60px 対 「規格をつくる側」33・「橋の上で働く人」32＝1.9倍）。
                   #    箱の幅ではなく**字数の差**なので、長い題を短くしてそろえる。
                   dict(x=0.50, y=0.42, t="規格の側",
                        c=J.INST, kind="org"),
                   dict(x=0.87, y=0.42, t="橋の作業員", c=J.ALERT,
                        kind="person")],
            edges=[dict(a=0, b=1, t="緊急の連絡手段を", c=J.DOC)],
            # 🔴 2026-09-10（⑤c' 4巡目・台帳 §Y-1-7）：右の赤い箱に線が1本も入らず、
            #    **その理由が画面のどこにも無かった**（`pr11` には同じ形の note が在る）。
            #    → 「無い線」を説明する1行を置く。出どころは同じ行の末尾へ畳む。
            note="橋の作業員へ入る線は、1本も無い（報告書 p.179）")),
    ),

    # ── ca08 ★決め所「橋の上には、その道が無かった」───────────
    # ⚠️ ⑤b-6：見出しが字幕と丸ごと同一だった（決め所はナレーションに任せる）
    "ca08": dict(
        t="片側にだけ、道がある",
        s="緊急を伝える道すじ",
        fig=("absent", dict(
            mode="pair",
            items=[dict(t="船の乗員と水先人", d="沿岸警備隊への無線がある",
                        ok=True, c=J.OK, n=1),
                   dict(t="橋の上で働く人", d="同じ道が無い", ok=False,
                        c=J.ALERT, n=0)],
            note="報告書 p.179")),
    ),

    # ── ca09 車の側への勧告 ───────────────────────────────
    # 図版 p173（Figure 71「架け替えたクイーン・イサベラ橋に付いた、点滅する赤い警告灯」）。
    # 🔴 ⑤b-5 で決めた。注記の「求めたこと＝**警報装置の検討**」＝ この写真がその装置そのもの。
    #    事故前のキー橋の絵を置くと「勧告が何を求めたか」が絵に出ない（場所の絵にしかならない）。
    # ⚠️ 副題は絵に合わせて直した（「事故の前のキー橋」→ 警告灯）。ナレーションは触っていない。
    "ca09": dict(
        # ⚠️ ⑤b-6：見出しは字幕と丸ごと同一、注記2本も 73%／100% だった（check_echo 3件）
        t="橋の持ち主へ、直接の求め",
        s="先に付いた橋がある　クイーン・イサベラ橋",
        photo=ss.page(173), **ss.kind(ss.page(173)), side="right", ann_y=336,
        ann=[dict(t="宛先", d="しきい値を超えた橋", dc=J.INST,
                  ds=30),
             dict(t="同じく", d="未計算の橋", dc=J.INST,
                  ds=30),
             dict(t="求めたこと", v="警報装置の検討", vc=J.DOC, vs=64)],
    ),

    # ── ca10 宛先に並ぶ名前 ───────────────────────────────
    # 🔴 台本の breakdown から panel へ。**「20」は画面に出さない**（台本 §9-2b。
    #    勧告の宛先をこちらが数えた値で、報告書の本文には無い）。数はナレーションが言う。
    "ca10": dict(
        # ⚠️ ⑤b-6：見出しは字幕の尻の切り取り、1段目は字幕に丸ごと含まれていた（check_echo 2件）
        t="名指しは、20の管理者",
        s="勧告の宛先に入っている橋",
        fig=("panel", dict(
            blocks=[dict(k="1", t="ゴールデンゲート橋", c=J.INST),
                    dict(k="2", t="マキナック橋の管理者", c=J.INST)],
            note="報告書 p.204")),
    ),

    # ── ca11 記録装置への勧告 ─────────────────────────────
    "ca11": dict(
        # ⚠️ ⑤b-6：副題は字幕と 90%、2段目と3段目は字幕に丸ごと含まれていた（check_echo 3件）
        t="記録が、途切れないように",
        s="VDR に求めたこと",
        fig=("panel", dict(
            blocks=[dict(k="1", t="停電中も止まらないこと", c=J.DOC),
                    dict(k="2", t="機関室と船橋の通話", c=J.DOC),
                    dict(k="3", t="マイクの別録り", c=J.DOC)],
            note="報告書 p.205")),
    ),

    # ── ca12 推定原因と寄与要因 ───────────────────────────
    "ca12": dict(
        t="推定原因と、寄与要因3つ",
        s="報告書がまとめた形",
        fig=("panel", dict(
            # 🔴 2026-09-09（⑤c'・型③＋型⑭）：**4件を3列で置いていたので、
            #    下の段が「1つだけ入って2つ空き」になっていた。**
            #    `check_space`（基準 `out/jiko/_empty.png`）で最大の空き矩形が
            #    **32.10%（x360〜1920・y360〜880）**＝本編でいちばん大きい穴。
            #    さらに `v`（推定原因）が**1段目にしか無い**ので、格子の枠の
            #    右下に打たれた「推定原因」が画面のまん中に浮いて、
            #    どの欄のものか分からなくなっていた（原寸で確認）。
            #    → ① `cols=2` にして 2×2 で埋める（空き欄が消える）
            #      ② `v` を落として4欄の作りをそろえる（型⑭）
            #    ⚠️ **4欄すべてに `v`（推定原因／寄与要因）を書く案は捨てた。**
            #      格子の枝は `vs = min(96, rh*0.34)` 対 `bs = min(72, rh*0.26)` で
            #      **`v` のほうが大きい**（数値を置く前提の級数）。区分ラベルを入れると
            #      「寄与要因」96px が中身 72px を食う＝型⑩と同じ逆転になる（実測）。
            #      区分は**見出し**（推定原因と、寄与要因3つ）と、`k` の札＋色が持つ。
            #    ⚠️ `k` の「因」は残す。ここは `panel` の**格子の枝**で、`k` は
            #      段の柱ではなく小さな札。赤／黄と対で区分を持たせている。
            # 🔴 2026-09-10（⑤c' 3巡目・§V-23／§V-25）── **`cols=1` は失敗だった。焼いて戻した。**
            #    §V-23（読む順・因と1〜3の種類の混在・下1/3の空き）を直そうと `cols=2 → 1`
            #    にしたが、r05 を原寸で見たら**明らかに悪化**していた：
            #    `panel` の格子は各セルの上に**セル幅いっぱいの色罫**を引くので、
            #    1列にすると罫が 1,750px の下線になり、短い文の右が丸ごと空く。
            #    ⚠️ **机上の門番は全部 0件のままだった**（[[feedback-desk-checks-dont-see-pictures]]）。
            #    → 2×2 に戻す。r04 を見比べると、因＝左上（赤）／1〜3（黄）の読む順は素直で、
            #      §V-23 の「読む順が壊れている」は**絵を見ると過大評価**だった。
            # 🔴 §V-25（段ラベルの混在）はラベルの体系をそろえて直す。
            #    「因」と「1」は**別の体系**（漢字1字 と 通し番号）だったので、
            #    「因／要1／要2／要3」＝**漢字1字＋番号の1体系**にする。
            #    ⚠️ ⑤c' 2巡目の「`k` の『因』は残す」という決めは活きたまま
            #      （[[feedback-new-rules-override-old]] で新しい規則を採ったのは体系のほう）。
            blocks=[dict(k="因", t="一本の信号線の緩み", c=J.ALERT),
                    dict(k="要1", t="橋に近すぎたこと", c=J.AMBER),
                    # ⚠️ ⑤b-6：2段目・3段目が字幕に丸ごと含まれていた（check_echo 2件）
                    dict(k="要2", t="橋の側の備えの欠如", c=J.AMBER),
                    dict(k="要3", t="作業員へ知らせる手段の欠如", c=J.AMBER)],
            cols=2,
            note="報告書 p.202")),
    ),

    # ══════════════════════════════════════════════════════
    #  締め（ep01–ep06）
    # ══════════════════════════════════════════════════════

    # ── ep01 最後の一つ ───────────────────────────────────
    # 実写 NTSB空撮（崩落した中央径間）
    "ep01": dict(
        # ⚠️ 2026-09-10（⑤c' 4巡目・台帳 §Y-5）：見出しの「**読む**」＝楽屋の言葉
        #    （画面が番組の進行を語っている）。3周目の「楽屋5件」で注記は直したが、
        #    **見出しが残っていた**。⚠️ 門番 B の語の一覧に「読む」は入っていない
        #    （「読むところ」は入っている）＝ 一覧で見る門番の穴。
        #    → 画面は中身を言う。読むのはナレーションの役。
        t="最後の一つは、知らせる手段",
        # 🔴 2026-09-10（⑤c' 3巡目・§V-31）：副題が「報告書の**言葉どおりに**」と
        #    約束するのに、答えは「即時の連絡手段が無い」＝**要約**だった（字幕は
        #    「実効的で即時の連絡手段が無かったこと」）。約束と中身が食い違う。
        #    → 副題は**どこの話か**だけにする（言葉どおりに読むのはナレーションの役）。
        s="寄与要因の三つ目",
        photo=ss.fb("ep01"), bias=0.5, side="right", ann_y=340,
        # 🔴 同・§V-15：注記の問いが「どこに書かれているか」「読むところ」＝
        #    **画面が視聴者に語りかける楽屋の言葉**だった。中身を問う形へ直す。
        # ⚠️ 「退避を知らせる即時の連絡手段」は字幕の一文の丸写しで `check_echo` が鳴った。
        #    **言葉どおりに読むのはナレーション**なので、画面は言い換えて短く持つ。
        ann=[dict(t="欠けていたもの", d="退避のための連絡の道",
                  dc=J.ALERT, ds=30),
             dict(t="届かなかった相手", d="橋の上で働いていた8人",
                  dc=J.LINE, ds=28)],
    ),

    # ── ep02 28秒 ─────────────────────────────────────────
    # ⚠️ `ticks` は**指示からの経過秒**。旗（時刻）と同じ語を軸に出さない（⑤b-2）。
    # ⚠️ ⑤b-6：見出しが字幕と 84%＝ほぼ同文だった。秒数は帯が持つ
    "ep02": dict(
        t="指示から、車が止まるまで",
        s="秒の目盛りで見る",
        fig=("timeline", dict(
            events=[dict(t=0, top="0127:53", t2="橋を閉めろという指示",
                         c=J.INST),
                    dict(t=28, top="0128:21", t2="両端で車が止まる", c=J.OK,
                         big=True)],
            t0=-4, t1=36,
            ticks=[(0, "0秒"), (10, "10秒"), (20, "20秒"), (30, "30秒")],
            band=[dict(a=0, b=28, t="28秒", c=J.AMBER)])),
    ),

    # ── ep03 ★決め所「素早い封鎖が、命を救った」───────────────
    # ⚠️ ⑤b-6：見出しが字幕と丸ごと同一だった（決め所はナレーションに任せる）
    "ep03": dict(
        t="止められた車が、あった",
        # 🔴 2026-09-10（⑤c' 3巡目・§V-15）：副題「**この動き**への評価」＝指示語＋楽屋の言葉。
        s="報告書の評価",
        fig=("panel", dict(
            # 🔴 2026-09-10（⑤c' 2巡目・§S-8 型⑭）：段1 だけ `v` が無く、
            #    右 600px が空いて段の右端が2種類になっていた。→ 問い／答えへ割る。
            # 🔴 同 3巡目・§V-17：答えが「橋を閉めた」「命を救われた」＝**動詞句**だった。
            #    答えは名詞句にし、段2 は問いと答えを入れ替えて向きをそろえる。
            # ⚠️ 2026-09-10（⑤c' 4巡目・台帳 §Y-5）：2つ直した。
            #    ① 見出し「止められた車が、あった」と段2の答え「橋に近づいていた運転者」が
            #       ほぼ同じことを言っていた → 条件は問いの側へ移す。
            #    ② そのせいで答えが 6字と11字になり、`fit` が縮めて**級数が2種**に割れていた。
            blocks=[dict(k="1", t="警官がしたこと", v="橋の通行止め", c=J.OK),
                    dict(k="2", t="助かったのは、橋に近づいていた", v="運転者",
                         c=J.OK)],
            note="報告書 p.175")),
    ),

    # ── ep04 掛からなかった電話 ───────────────────────────
    "ep04": dict(
        t="同じ時刻に、掛からなかった",
        s="橋の上へ向かう電話",
        fig=("absent", dict(
            mode="single",
            # ⚠️ 札を「橋の上へ向かう電話」にすると副題を100%覆う（check_dup）。
            items=[dict(t="指示と同じ時刻に、点検員へ掛ける電話", d="無い",
                        ok=False, c=J.ALERT)],
            lead="番号は交換されていた",
            note="報告書 p.177")),
    ),

    # ── ep05 報告書の結び ─────────────────────────────────
    # 実写 崩落現場（郡Flickr）
    "ep05": dict(
        # ⚠️ ⑤b-6：見出しは字幕と丸ごと同一、注記は 82% だった（check_echo 2件）
        t="結びは、連絡の話だった",
        s="最後の一文",
        # 🔴 2026-09-10（⑤c' 3巡目・§V-18）：**絵に橋がほとんど写っていない**。
        #    r04 を原寸で見ると、崩れた橋桁は**画面の左端**だけで、あとは一面の水。
        #    しかも `side="left"` なので、**唯一の中身の上に注記を重ねていた**。
        #    → 注記を右（空いている水面の側）へ移す。切り出しは変えない
        #      （`side` は暗幕と注記の側だけを決める。`bias` は触っていない）。
        photo=ss.fb("ep05"), bias=0.5, side="right", ann_y=340,
        # 🔴 同・§V-30：問い「**何が**」が単独では意味不明で、しかも
        #    「何に欠かせないか」→「何が」の順＝**結論が先・主語が後**で逆だった。
        #    → 主語（欠かせないもの）を先に置く。
        ann=[dict(t="欠かせないとされたもの", d="退避のための即時の連絡",
                  dc=J.ALERT, ds=30),
             dict(t="何に欠かせないのか", v="橋で働く人の安全", vc=J.DOC,
                  vs=56)],
    ),

    # ── ep06 一本の線と、一本の電話 ───────────────────────
    # 写真 ss.PRE_2019（2019年の全景）。⑤b-5 で決めた。
    # 🔴 c101（港の出口を、跨いでいた）の1枚目と**同じ写真**で閉じる。約40分あく。
    #    最後の1カットだけは、崩れる前の橋に戻る。
    "ep06": dict(
        t="一本の線と、一本の電話",
        s="259ページが指しているもの",
        photo=ss.PRE_2019, bias=0.5, side="right", ann_y=340,
        ann=[dict(t="緩んでいた信号線", d="船から推進と操舵を奪った", dc=J.ALERT,
                  ds=30),
             dict(t="掛からなかった電話", d="橋の上へ知らせが届かなかった",
                  dc=J.ALERT, ds=28)],
    ),
}
