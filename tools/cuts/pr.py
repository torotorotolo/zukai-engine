# -*- coding: utf-8 -*-
"""冒頭の引き pr01–pr12（12カット）。6本目＝フランシス・スコット・キー橋 崩落。

■ この章の役目
  🔴 **冒頭46秒に何を置くかが、この動画でいちばん効く**（3本目の実測で、
     冒頭46秒に21〜42%が消えている → [[reference-jiko-third-video-numbers]]）。
  台本 §4 の設計＝ pr01 いつ・どこ・何が／pr02 橋の上に8人／pr03 6人が戻らなかった／
  **pr04（約40秒）でひっくり返す**＝「警官には届き、8人には届かなかった」＋★決め所。

■ 出どころ（画面に出る出典は素材の側が持つ。ここは台本 §4 の出典欄）
  p12（骨格）／p35・p37（軌跡）／p48・p49（接触と崩落）／p110（調査）／
  p125・p145・p202（信号線）／p175・p177（通信）／p192・p194（脆弱性）。

■ 🔴 素材の実測（⑤b・2026-09-08）
  ・報告書の図は `ss.page(印字ページ)` で引く。**切り出しは自動**
    （`ref/keybridge/textbands.json` の図の矩形 → `scene_jiko.TRIM_BY_PHOTO`）。
  ・実写の欄は `ss.fb(cid)` ＝ 動画のひかえの静止画。**どの動画の何秒目かは
    `footage.USE` が持つ**（⑤b の続きで書く。台帳＝`ref/keybridge/SHOTS_INDEX.md`）。
"""
import jiko_style as J
import cuts.ss as ss

SPEC = {

    # ── pr01 いつ・どこ・何が落ちたか ───────────────────────
    # 実写 NTSB空撮（崩落した中央径間・2024-03-26）
    "pr01": dict(
        # ⚠️ ⑤b-6：副題が字幕の1文と 87%＝ほぼ同文だった。日時は段の時計が持つ
        t="橋は、真ん中から落ちた",
        s="崩落の現場",
        photo=ss.fb("pr01"), bias=0.5, side="right", ann_y=340,
        # 🔴 2026-09-10（⑤c' 3巡目・§V-17）：答えが「全長2,769メートルの橋の、中央の3径間」
        #    ＝**読点入りの長い句**で、名詞句1つになっていなかった。
        #    ⚠️ 全長2,769メートルは**字幕が言う**（[[feedback-no-subtitle-when-onscreen]]）ので
        #      画面から落とす。§T-3（画面カンマ有・字幕カンマ無）の1件もここで消える。
        ann=[dict(t="現地時間", v="0129", vc=J.INK_W, vs=120),
             dict(t="落ちたもの", v="中央の3径間", vc=J.ALERT, vs=88)],
    ),

    # ── pr02 橋の上に8人いた ───────────────────────────────
    # 実写 USCG 応急艇（未明の現場に着く・2024-03-26）
    # ⚠️ ⑤b-6：見出しが字幕と 89%＝ほぼ同文だった
    "pr02": dict(
        t="働いていたのは、8人",
        # 🔴 2026-09-10（⑤c' 3巡目・§V-18）：副題が**写っていないものを名乗っていた**。
        #    r04 を原寸で見ると、絵は未明の水面・崩れたトラス・コンテナを積んだダリで、
        #    **応急艇はどこにも写っていない**。⚠️ 素材は `fb_*.jpg`＝ひかえの静止画なので
        #    門番は1件も鳴らない（[[feedback-fallback-stills-must-match-the-era]]）。
        s="未明の現場に残った橋桁とダリ",
        photo=ss.fb("pr02"), bias=0.5, side="left", ann_y=336,
        # 🔴 同・§V-16：補足が「**それ**を見て回っていた」＝指示語で、字幕の一文とほぼ同じ。
        ann=[dict(t="道路補修班", v="7人", d="道路の穴を埋めていた",
                  vc=J.AMBER, vs=104, dc=J.LINE, ds=30),
             dict(t="点検員", v="1人", d="打ったコンクリートの点検",
                  vc=J.AMBER, vs=88, dc=J.LINE, ds=30)],
    ),

    # ── pr03 6人が戻らなかった ─────────────────────────────
    # 実写 NTSB空撮（水面に沈んだトラスと工事車両）
    "pr03": dict(
        # 🔴 2026-09-10（⑤c' 4巡目・台帳 §Y-2）：門番 A を直したら鳴った12件の1つ。
        #    見出し「**6人**が、戻らなかった」が段1の答え「**6人**」を先に言っていた。
        #    ⚠️ 旧 A は `len(v)<3 or 数字だけ` で**数字の答えを判定から外していた**ので
        #      ずっと 0件だった（§Y-3-1）。
        #    → この章の他のカットと同じ扱いにする＝**見出しは数を言わず、数は段が持つ**
        #      （`pr01`「日時は段の時計が持つ」・`pr08`「数は段に任せる」と同じ）。
        t="戻らなかった人が、いた",
        # 🔴 2026-09-10（⑤c' 3巡目・§V-18）：副題の「工事車両」が絵に無い。
        #    r04 を原寸で見ると、水面に横たわるトラスとダリの船首だけ。
        s="水面に沈んだトラスと、ダリの船首",
        photo=ss.fb("pr03"), bias=0.5, side="right", ann_y=326,
        # 🔴 同・§V-32：注記3つが同じ体裁で並ぶのに、1・2は**橋の上**の8人、
        #    3つ目だけ**船側**（乗員23人）の別の集合で、23人が8人の内訳に見えていた。
        #    → 1・2 の問いに「橋の上」を入れて、どちらの集合かを画面で分ける。
        ann=[dict(t="橋の上で亡くなった", v="6人", vc=J.ALERT, vs=104),
             dict(t="橋の上から生きて戻った", v="2人", d="重傷1人・点検員は無事",
                  vc=J.OK, vs=88, dc=J.LINE, ds=30),
             # ⚠️ 2026-09-10（⑤c' 4巡目・台帳 §Y-5）：3段目だけ答えが「けが1人」＝
             #    **名詞＋数**で、他の2段（「6人」「2人」＝数＋人）と体系が違っていた。
             dict(t="船の乗員23人のうち、けがは", v="1人", vc=J.INK_W, vs=72)],
    ),

    # ── pr04 🔴 ひっくり返す。★決め所「衝突まで、1分16秒あった」──────
    # 🔴 この動画でいちばん効くカット（約40秒）。**物証は分秒**なので timeline。
    #    秒は 0127:00 を 0 とした相対値（報告書 p175・p177 の Event Sequence）。
    "pr04": dict(
        t="1分16秒、誰も伝えなかった",
        s="橋の両端の警官と、橋の上の8人",
        fig=("timeline", dict(
            events=[dict(t=53, top="0127:53", t2="船から通報", c=J.DOC),
                    dict(t=81, top="0128:21", t2="橋の両端を封鎖", c=J.INST),
                    dict(t=129, top="0129:09", t2="橋脚に接触", c=J.ALERT,
                         big=True)],
            # ⚠️ 2026-09-08（⑤b-2）：目盛りに旗と同じ時刻を渡していたので、
            #    同じ語が軸と旗に二度出ていた（check_dup が3件）。目盛りは
            #    0127:00 からの経過秒にして、旗（時刻）と役目を分ける。
            t0=40, t1=140,
            ticks=[(60, "60秒"), (90, "90秒"), (120, "120秒")],
            band=[dict(a=53, b=129, t="1分16秒", c=J.AMBER)])),
    ),

    # ── pr05 調べたのは誰か ────────────────────────────────
    # 実写 NTSB 調査員（Dali 船内・2024-03-28）
    "pr05": dict(
        # 🔴🔴🔴 2026-09-10（⑤c' 3巡目・§V-20）**同じ動画の中で日数が食い違っていた。**
        #    ここが「船に乗り込んだのは／**事故の2日後**」、`c701` が「調査員が乗り込んだ日／
        #    **4月1日**」＝ 事故は3月26日なので6日後。どちらも「乗り込んだ」と同じ語で、
        #    画面だけでは別の出来事かどうか分からなかった。
        #    → **原文 MIR-25-40 に当たった**（`ref/keybridge/ntsb_MIR2540.txt`）：
        #      ・1635行 “…until postaccident examinations began on **April 1**.”
        #      ・3875行 “**Between April 1 and April 29, 2024**, NTSB and Coast Guard
        #        investigators … conducted postaccident … examinations on board the vessel”
        #      ・3883行 “**On April 1**, investigators … attempted to close the HV breaker, HR1”
        #      ＝ **`c701` の4月1日が原文どおり**（ただし「船内の試験が始まった日」）。
        #      「事故の2日後」は **原文に無い**。素材の側の記述も `ref/keybridge/clips.json` の
        #      note が “**March 27**, 2024” ・`date` 欄が 2024-03-28（＝掲載日）で、
        #      **1日後とも2日後とも読める**＝画面に出せる数字ではない
        #      （[[feedback-secondhand-numbers-differ-from-the-original]]）。
        #    → 日数の段を落とし、**報告書そのものの日付**（原文33行 “November 18, 2025”）に替える。
        # 🔴 同・§V-14：見出し「259ページが、残った」が段1の答えを先に言っていた。
        t="残ったのは、一冊の報告書",
        # ⚠️ 副題に調査機関の名を置くと、右上の**出典の行と同じ語**になる（⑤b-2）。
        s="ダリに乗り込んだ調査員",
        photo=ss.fb("pr05"), bias=0.5, side="right", ann_y=350,
        # ⚠️ 2026-09-08（⑤b-2）：`t`＋`v`＋`d` の3つを1段に詰めていたので、
        #    96px の値が段の見出しに 138×7px 食い込んでいた。文書番号は見出しへ畳んだ。
        ann=[dict(t="報告書 MIR-25-40", v="259ページ", vc=J.DOC, vs=88),
             dict(t="公表されたのは", v="2025年11月18日", vc=J.AMBER, vs=64)],
    ),

    # ── pr06 原因は一本の信号線 ────────────────────────────
    # 図 p125（端子台に差し込まれた線の深さ）
    # ⚠️ 台本の出典欄は p145・p202（本文の根拠）。**画面に出る出典は図のページ**。
    "pr06": dict(
        t="原因は、一本の信号線だった",
        s="端子台381に差し込まれた Wire 1 と Wire 3",
        photo=ss.page(125), **ss.kind(ss.page(125)), side="right", ann_y=330,
        ann=[dict(t="入っていなかったもの", d="線の先が、端子台の奥まで",
                  dc=J.ALERT, ds=34),
             dict(t="場所", d="船の高圧配電盤の制御回路", dc=J.LINE, ds=30)],
    ),

    # ── pr07 推進と操舵を同時に失う ─────────────────────────
    # 図 p37（出港から接触までの軌跡）
    # ⚠️ ⑤b-6：見出しが字幕の1文の尻の切り取りだった
    "pr07": dict(
        t="動かす力を、まとめて失う",
        s="出港から接触までの軌跡",
        photo=ss.page(37), **ss.kind(ss.page(37)), side="right", ann_y=336,
        ann=[dict(t="橋まで", v="3,200フィート", d="およそ975メートル",
                  vc=J.AMBER, vs=96, dc=J.LINE, ds=30),
             # 🔴 2026-09-10（⑤c' 3巡目・§V-17）：問いが「止まった」＝**動詞**で、
             #    答えの名詞句と噛み合っていなかった。問いのほうを名詞にする。
             dict(t="止まったもの", d="主機関と、舵を動かす力", dc=J.ALERT, ds=32)],
    ),

    # ── pr08 13秒で橋脚が崩れた ────────────────────────────
    # 実写 240330-A-PA223（Dali の船首と落ちた橋桁）
    # ⚠️ ⑤b-6：見出しが字幕と 95%＝ほぼ同文だった。13秒は段が持つ
    "pr08": dict(
        # 🔴 2026-09-10（⑤c' 3巡目・§V-14）：見出し「中央の3径間」が段2の答え「3つ」を
        #    先に言っていた。見出しは**速さ**だけを言い、数は段に任せる。
        t="落ちるまでは、一瞬だった",
        s="ダリの船首の上に落ちた中央の橋桁",
        photo=ss.fb("pr08"), bias=0.5, side="left", ann_y=336,
        # 🔴 同・§V-17：補足「船首の上と、川へ」＝**方向**で名詞句でない。
        ann=[dict(t="接触から崩落まで", v="13秒", vc=J.ALERT, vs=112),
             dict(t="落ちた径間", v="3つ", d="船首の上と、川の中", vc=J.AMBER,
                  vs=88, dc=J.LINE, ds=30)],
    ),

    # ── pr09 備えが無かった ────────────────────────────────
    # absent single（脆弱性の計算をした欄が空）
    "pr09": dict(
        t="崩れないための備えが無い",
        s="外航船の衝突に対する脆弱性の評価",
        fig=("absent", dict(
            mode="single",
            items=[dict(t="船が当たったときの脆弱性の評価",
                        d="行われた記録が無い", ok=False, c=J.ALERT)],
            lead="橋の設計指針が求めているもの",
            # ⚠️ ⑤b-6：note が字幕に丸ごと含まれていた。出どころだけにする
            note="報告書 MIR-25-40")),
    ),

    # ── pr10 許容の線のおよそ30倍 ──────────────────────────
    # compare（キー橋の年間崩落頻度 vs 許容の線）
    # 🔴 台本 §9-3：**「30倍」だけを図に書かない。0.002921 と 0.0001 を出す。**
    #    → [[feedback-charts-lie-with-their-defaults]]（数は宣言する）
    # ⚠️ `vmax` を渡さないと大きいほうが満杯の棒になる（c611 の実例）。外の基準で測る。
    "pr10": dict(
        t="許容の線の、およそ30倍",
        s="年間の崩落頻度（事故のあとに計算した値）",
        fig=("compare", dict(
            items=[dict(v=0.002921, t="キー橋（NTSB の計算）",
                        disp="0.002921", unit="／年", c=J.ALERT,
                        sub="入航 0.001405 ＋ 出航 0.001516"),
                   dict(v=0.0001, t="重要な橋に許される線",
                        disp="0.0001", unit="／年", c=J.INST,
                        sub="年に1万分の1")],
            vmax=0.0035, ratio="およそ30倍", bar=False,   # 🔴 ⑤b-2：値の比 1:35 で棒が嘘をつくと check_layout が落ちた。数字に語らせる
            ref="橋の設計指針（AASHTO）が定めるしきい値",
            note="誰も計算していなかった")),
    ),

    # ── pr11 最後の一本だけが繋がっていなかった ──────────────
    # people（水先人→配車係→当直→警官／橋の上へは線が無い）
    # 🔴 「無い線」を描かない。**節を置いて、そこへ入る線が1本も無い**ことで見せる。
    "pr11": dict(
        t="最後の一本が、無かった",
        s="船から陸へ、陸から警官へ渡った知らせ",
        fig=("people", dict(
            # 🔴🔴 2026-09-10（⑤c' 3巡目・§V-8-1／§V-24）**主役がいちばん小さかった。**
            #    「橋の両端の警官」20px・「橋の上の8人」21px に対し、脇の「水先人」40・
            #    「配車係」40・「当直」48。＝ このカットの主役2つが最小。
            #    真因は端の x=0.06 で箱の幅が下限 300px に張りついていたこと
            #    （`cuts/c2.py` の c204 に理由を書いた）。
            #    実測：箱 300 → 491px・最小級数 20 → 42px・開き 28 → 14px。
            #    ⚠️ 残る 14px は字数の差（「当直」2字 対「橋の両端の警官」7字）なので、
            #      短すぎる題を役どころまで書いてそろえる（「当直」→「州交通局の当直」）。
            nodes=[dict(x=0.13, y=0.16, t="水先人", d="ダリの船橋",
                        c=J.INK_W, kind="person"),
                   dict(x=0.50, y=0.16, t="配車係", d="水先人組合",
                        c=J.LINE, kind="org"),
                   dict(x=0.87, y=0.16, t="州交通局の当直", d="運用室",
                        c=J.INST, kind="org"),
                   dict(x=0.87, y=0.72, t="橋の両端の警官", d="車を止めた",
                        c=J.OK, kind="person"),
                   dict(x=0.13, y=0.72, t="橋の上の8人", d="",
                        c=J.ALERT, kind="person")],
            edges=[dict(a=0, b=1, t="0127:53", c=J.DOC),
                   dict(a=1, b=2, t="0127:57", c=J.DOC),
                   dict(a=2, b=3, t="0128:21　封鎖", c=J.INST)],
            note="橋の上の8人へ入る線は、1本も無い")),
    ),

    # ── pr12 この動画がたどるもの ──────────────────────────
    # ⚠️ ⑤b-6：見出しが字幕と 86%＝ほぼ同文だった。ページ数は note が持つ
    "pr12": dict(
        # 🔴 2026-09-10（⑤c' 3巡目）ここは3件まとめて直した。
        #    ・§V-15 副題「この動画が追うもの」＝**楽屋の言葉**（画面が視聴者に語りかける）
        #    ・§V-14 見出し「1つだけ」が `lead`「…だけ」を先に言う
        #    ・§V-37 `lead` と `note` で「**MIR-25-40**」が1枚に2度出ていた
        #      （`c110` のページ番号2度と同型）→ 番号は `note` の1か所だけにする
        t="報告書から、三つを取り出す",
        s="停電、通報、原因",
        fig=("panel", dict(
            # 🔴 2026-09-10（⑤c' 2巡目・§T-8）：**3段すべて役が逆**だった
            #    （`t`＝値・`v`＝ラベル）。同じ冒頭12カットの `c918`（`t`「費用」）と
            #    `ep03` は 問い→答えの向きなので、**1本の動画の中で両方の向きが出ていた**。
            # ⚠️ 段3 は値とラベルの関係ですらなかった（「一本の信号線」は**原因**であって
            #    「原因が見つかるまでの手順」ではない）。問いのほうを直す。
            blocks=[dict(k="1", t="停電から接触まで", v="4分9秒", c=J.AMBER),
                    dict(k="2", t="通報から接触まで", v="1分16秒", c=J.ALERT),
                    # 🔴 §V-8-2（答えの書体の割れ）：段1・段2 は数字なので Dela、
                    #    段3「一本の信号線」だけ数字が無く **Noto に落ちて**いた
                    #    （`numfam()` は漢字がつぶれるので数字なしを Noto に落とす）。
                    #    ＝ 同じ役の答えが1枚で2書体。→ **数の形にそろえる**。
                    # 🔴🔴 2026-09-10（⑤c' 4巡目・台帳 §Y-1-1）：**上の直しがつぶれを作った。**
                    #    「信号線1本」は数字を含むので `numfam()` が Dela のまま残し、
                    #    原寸で見ると**「線」の白い抜けがゼロ**になっていた。
                    #    ＝ 書体をそろえるために、読めない字を作っていた。
                    #    → 漢字は問いの側へ移し、答えは**数だけ**にする。
                    #      3段とも Dela のまま（書体は割れない）・つぶれる字は無くなる。
                    dict(k="3", t="原因として残った信号線", v="1本",
                         c=J.DOC)],
            lead="使うのは NTSB の最終報告だけ",
            note="報告書 MIR-25-40（2025年）259ページ")),
    ),
}
