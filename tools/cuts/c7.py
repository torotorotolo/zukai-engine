# -*- coding: utf-8 -*-
"""第7章 そうではなかったもの c701–c726（26カット）。

■ この章の役目
  **追うのは、87パークの土と構造の解析。噂を出しっぱなしにしないための章。**
  🔴 噂を語るカット（c703 c709）には「噂」の札を出す（ルール §5＝噂と事実を画面で分ける）。
  決め所は c717「傷んだつなぎ目を壊すにも、小さすぎた」・c722「大きく寄与してはいない」。

■ 出どころ
  技術的知見 p184（数字だけ。Google の画像は出さない）・p185・p189／NIST ニュース 2026-06-22／B-Roll #1・#2・#5。
  ⚠️ 87 Park は建物の**南**隣（p3 で確認）。字幕は「エイティセブン・パーク」、画面の札は `87 Park`。
"""
import jiko_style as J
import cuts.ss as ss

RUMOR = dict(t="■ 言われていること", d="確かめられていない、噂の段階の話", c=J.ALERT, ts=40,
             dc=J.LINE, ds=30)

SPEC = {

    # ── c701 24通り ───────────────────────
    "c701": dict(
        t="検討した筋書きは、24通り",
        s="NIST ニュース 2026年6月22日",
        fig=("compare", dict(
            items=[dict(v=24, t="検討した筋書き", disp="24", unit="通り", c=J.LINE),
                   dict(v=1, t="残った筋書き", disp="1", unit="つ", sub="プールデッキの押し抜き", c=J.ALERT)],
            note="※ どこで、なぜ始まったかについて（NIST ニュース）")),
    ),

    # ── c702 残らなかった23通り ────────────────
    "c702": dict(
        t="落とされた説の中身",
        s="p189 の一覧の前に",
        fig=("absent", dict(
            mode="seat", lead="24通りの筋書き",
            items=[dict(t="残った", d="プールデッキの押し抜き", ok=True, c=J.OK),
                   dict(t="残らなかった", d="23通り", ok=False, c=J.LINE)],
            note="5つを次から見ていく（p189）")),
    ),

    # ── c703 いくつもの説（噂の札）─────────────────
    "c703": dict(
        t="起きた直後から、いくつもの説",
        s="現場の引き（NIST 記録映像）",
        photo=ss.fb("c703"), bias=0.5, side="right", ann_y=340,
        ann=[RUMOR],
    ),

    # ── c704 噂であることを明示 ─────────────────
    "c704": dict(
        t="確かめられてはいない、噂の段階",
        s="第7章で答える噂",
        fig=("panel", dict(
            lead="■ 言われていることの出どころ",
            blocks=[dict(k="噂", t="報道", c=J.LINE),
                    dict(k="噂", t="住民の話", c=J.LINE),
                    dict(k="噂", t="裁判のなか", c=J.LINE)],
            cols=3, note="※ ここは噂。出どころのある事実ではない。この章は、これに NIST の解析で答える")),
    ),

    # ── c705 隣の工事（87 Park）────────────────
    "c705": dict(
        t="すぐ南側に、別の高層マンション",
        s="87 Park と現場（NIST 空撮の1コマ）",
        photo=ss.S_87PARK, side="right", ann_y=340, bias=0.45, xbias=0.15, zoom=1.4,
        ann=[dict(t="87 Park", d="建物のすぐ南側に建設中だった", dc=J.INK_W, ds=32)],
    ),

    # ── c706 敷地の境界（自作）─────────────────
    "c706": dict(
        t="地面を掘り、鋼の板を打ち込んだ",
        s="敷地の境界（模式）",
        fig=("mapfig", dict(
            lead="北が上・縮尺なし",
            points=[dict(x=0.50, y=0.30, t="Champlain Towers South", d="崩れた建物", c=J.INK_W,
                         kind="part"),
                    dict(x=0.50, y=0.74, t="87 Park", d="建設中（掘削・鋼矢板）", c=J.ALERT,
                         kind="part")],
            link=(0, 1), scale="最短 約9フィート",
            note="距離は p184 の数字だけ（画像は Google のため出さない）")),
    ),

    # ── c707 約9フィート ─────────────────
    "c707": dict(
        t="最短で、およそ9フィート",
        s="NIST が測った距離（p184）",
        fig=("compare", dict(
            items=[dict(v=9, t="打ち込み位置 ⇄ 建物の壁", disp="約9", unit="ft", sub="最短", c=J.ALERT),
                   dict(v=150, t="建物の短辺", disp="150", unit="ft", sub="p3", c=J.LINE)],
            note="※ 9フィート＝NIST の実測（p184）")),
    ),

    # ── c708 9フィート＝約2.7メートル ────────────
    "c708": dict(
        t="2.7メートル、という近さ",
        s="9フィートをメートルに",
        fig=("compare", dict(
            items=[dict(v=2.7, t="最短距離", disp="2.7", unit="m", sub="約9フィート", c=J.ALERT)],
            vmax=10, ref="10 m",
            note="※ 人が両手を広げた幅の、およそ1.5倍")),
    ),

    # ── c709 住民の証言（噂の札）────────────────
    "c709": dict(
        t="住民は、揺れを感じたと話した",
        s="残った棟＝住民の建物（NIST 記録映像）",
        photo=ss.fb("c709"), bias=0.5, side="left", ann_y=340,
        ann=[RUMOR],
    ),

    # ── c710 そこから出た説 ──────────────────
    "c710": dict(
        t="工事の振動が、傷めたのではないか",
        s="噂から出た説",
        fig=("panel", dict(
            lead="■ 言われていること",
            blocks=[dict(k="噂", t="隣の工事の振動が原因？", c=J.LINE)],
            cols=1, note="※ ここは噂。このあと NIST の解析で答える")),   # 「次のカット」は制作の用語
    ),

    # ── c711 土と構造の解析モデル ───────────────
    "c711": dict(
        t="噂を、計算で確かめた",
        s="解析モデル（p185）",
        photo=ss.P185, side="right", ann_y=330, color=0.4,
        # 🔴 2026-09-06（⑤c' 直す #2）：H-05/H-06：右 1/3 に p185 の箇条3つと別写真が入り込んでいた（left 0・cw 1744＜箇条の行頭 1753）
        #    切り方は `check_slide.py` の粗の数がいちばん少ない値を総当たりで探した
        xbias=0.0, bias=0.325, zoom=1.6,
        ann=[dict(t="Soil-Structure Interaction", d="地面と建物を一緒に解く", dc=J.INST, ds=30)],
    ),

    # ── c712 層の分解図 ───────────────────
    "c712": dict(
        t="骨組み・地下・基礎・地盤に分けた",
        s="層の分解（p185）",
        photo=ss.P185, side="right", ann_y=330, color=0.4,
        # 🔴 2026-09-06（⑤c' 直す #2）：H-07/H-08：注記の層の名前が絵の層とずれ、灰字 basement と同じ行に重なっていた
        xbias=0.025, bias=0.35, zoom=1.7,
        # 2026-09-06（6）G-09：t が画面の焼き込みの層名の写しだった（superstructure /
        #   substructure / soil-rock は原画に焼き込まれている）。訳だけを出す
        ann=[dict(t="上の骨組み", ts=34),
             dict(t="地下・基礎", ts=34),
             dict(t="地盤", ts=34)],
    ),

    # ── c713 地盤の層 ─────────────────
    # 🔴 2026-09-06（⑤c H-10）：**絵と話が別物**だった。ナレーションは「この土地の
    #    地盤の性質」なのに、当てていたのは B-Roll #5 の**倉庫でコンクリートのコアに
    #    計測器をあてる手元**（153〜159秒）。B-Roll に地盤の計測は1コマも無い。
    #    ⚠️ 台本の文言は変えない方針なので、**絵のほうを話に合わせる**＝p185 の分解図の
    #    いちばん下の層（地盤）へ寄る。c712 は同じ図の全体なので、寄りで分ける
    "c713": dict(
        t="地盤の性質が、計算に入っている",
        s="解析モデルのいちばん下の層（p185）",
        photo=ss.P185, side="right", ann_y=360, color=0.4,
        xbias=0.0, bias=0.80, zoom=1.86,
                # 2026-09-06（6）G-14：注記が焼き込みの英文の上に載っていた。dy で逃がす（枠外にならないことは check_layout の実測箱で確認）
        ann=[dict(t="伝わり方", dy=80, d="地盤で、伝わり方が別物になる", dc=J.LINE, ds=30)],
    ),

    # ── c714 1つめの結論 ─────────────────
    "c714": dict(
        t="答えは、3つ",
        s="p185 の右の箇条　1つめ",
        photo=ss.P185, side="left", ann_y=470, color=0.4,
        # 🔴 2026-09-06（⑤c' 直す #2）：H-11：left が箇条の行頭 x=1753 を越えて**英文が全行、左端で語の途中から**始まっていた。k=0 と k=1 の両端で left≤1753 になる値
        xbias=0.96, bias=0.78, zoom=1.9,
        # ⚠️ 「N つめ」を言うのに画面に印が無かった（H-11/H-15/H-19）。
        #    型に「箇条の1行だけ明るくする」手段は無いので、注記を箇条1（画面 y 424〜660）の高さへ置いて印にする
        ann=[dict(t="1", d="振動の減り方", dc=J.INK_W, ds=32)],
    ),

    # ── c715 basement・substructure の層に寄る ────────
    "c715": dict(
        t="地下と鋼の壁が、振動を減らした",
        s="左の3Dの地下と基礎（p185）",
        photo=ss.P185, side="right", ann_y=330, color=0.4,
        # 🔴 2026-09-06（⑤c' 直す #2）：H-13/H-14：注記を1段に減らしたうえで、灰字 basement と重ならない位置へ
        #    切り方は `check_slide.py` の粗の数がいちばん少ない値を総当たりで探した
        xbias=0.2, bias=0.5, zoom=2.4,
        # 🔴 H-13：「sheet pile wall」は p185 の図に**ラベルも絵も無い**（箇条の本文にしか
        #    出てこない）＝絵に無い物を注記が言っていた。1段に減らす
        # ⚠️ H-14：t が画面の灰字 "basement" の真上に載っていた＝英語をやめて日本語に
        ann=[dict(t="南側の地下", d="basement＝地下（p185 の左の3D）", dc=J.OK, ds=30)],
    ),

    # ── c716 2つめの結論 ─────────────────
    "c716": dict(
        t="届いた振動の大きさを、計算した",
        s="p185 の右の箇条　2つめ",
        photo=ss.P185, side="left", ann_y=510, color=0.4,
        # 🔴 2026-09-06（⑤c' 直す #2）：H-15：同上（箇条の行頭 x=1753）。指すのは箇条2なので下寄せ
        xbias=0.96, bias=1.0, zoom=1.9,
        # ⚠️ 「N つめ」を言うのに画面に印が無かった（H-11/H-15/H-19）。
        #    型に「箇条の1行だけ明るくする」手段は無いので、注記を箇条2（画面 y 469〜715）の高さへ置いて印にする
                # 2026-09-06（6）G-14：注記が焼き込みの英文の上に載っていた。dy で逃がす（枠外にならないことは check_layout の実測箱で確認）
        ann=[dict(t="2", dy=80, d="つなぎ目に届いた振動の大きさ", dc=J.INK_W, ds=32)],
    ),

    # ── c717 ★決め所「傷んだつなぎ目を壊すにも、小さすぎた」──────
    "c717": dict(
        t="届いた振動は、壊す大きさではない",
        s="p185 の2つめの文",
        fig=("quote", dict(
            phrase="傷んだつなぎ目を壊すにも、小さすぎた",
            rows=[("誰が", "NIST（土と構造の解析）", J.INST),
                  ("いつ", "2026年6月22日 公表", J.LINE),
                  ("どこに", "技術的知見 スライド185", J.DOC)],
            ctx="原文 Computed vibrations at critical slab-column connections of CTS were too small to damage even the distressed connections.",
            paper=True)),
    ),

    # ── c718 3項目の引き（3つめを明るく）────────────
    "c718": dict(
        t="みっつめが、いちばん大事",
        s="p185 の右の3項目",
        # ⚠️ H-17：注記2段が箇条1の本文の上に載っていた。箇条の左の余白は 248px しか
        #    無く side も動かせないので、**1段に減らして、指す箇条3と同じ高さ**に置く
        #    （箇条3は画面 y 736〜935。ann_y=760 で注記は 760〜870＝帯 892 の内側）
        photo=ss.P185, side="left", ann_y=760, color=0.4,
        **ss.focus(ss.P185, 0.83, 0.74, 1.6),
        ann=[dict(t="3つめ", d="住民の体感について", dc=J.INK_W, ds=34)],
    ),

    # ── c719 3つめの文の寄り ─────────────────
    "c719": dict(
        t="住民の体感を、解析が裏付けた",
        s="p185 の3つめの文",
        photo=ss.P185, side="left", ann_y=670, color=0.4,
        # 🔴 2026-09-06（⑤c' 直す #2）：H-19：決め所 Analyses corroborate の行頭が枠外だった。箇条3が字幕帯の上に収まる最小の zoom
        xbias=0.9, bias=1.0, zoom=2.1,
        # ⚠️ H-20：d が画面の同じ英文の写し（G-09）。写さず**語の読み方**を足す
        # ⚠️ 「N つめ」を言うのに画面に印が無かった（H-11/H-15/H-19）。
        #    型に「箇条の1行だけ明るくする」手段は無いので、注記を箇条3（画面 y 629〜889）の高さへ置いて印にする
        ann=[dict(t="原文の語", d="corroborate＝裏づける", dc=J.INK_W, ds=30)],
    ),

    # ── c720 感じる大きさと壊れる大きさ ───────────
    "c720": dict(
        t="感じる大きさは、壊す大きさのはるか下",
        s="人と建物のしきい値（p185）",
        fig=("compare", dict(
            items=[dict(v=1, t="人が感じ取れる振動", disp="低い", unit="", c=J.LINE),
                   dict(v=2, t="建物を壊す振動", disp="はるかに上", unit="", c=J.ALERT)],
            bar=False, note="原文 well below levels that would damage the structure")),
    ),

    # ── c721 この答えの形 ─────────────────
    "c721": dict(
        t="感じたことと、原因であることは別",
        s="この答えの形",
        fig=("absent", dict(
            mode="pair", lead="住民の証言は",
            items=[dict(t="うそ・思い違い", d="ではない", ok=False, c=J.LINE),
                   dict(t="本当に感じた", d="ただし、原因ではない", ok=True, c=J.OK)])),
    ),

    # ── c722 ★決め所「大きく寄与してはいない」───────────
    "c722": dict(
        t="こうした説を、1枚にまとめて",
        s="p189 の見出し",
        fig=("quote", dict(
            phrase="大きく寄与してはいない",
            rows=[("誰が", "NIST", J.INST),
                  ("いつ", "2026年6月22日 公表", J.LINE),
                  ("どこに", "技術的知見 スライド189 の見出し", J.DOC)],
            ctx="原文 Things that did not contribute significantly to the collapse",
            paper=True)),
    ),

    # ── c723 5項目（前半）─────────────────
    "c723": dict(
        t="並んでいるのは、5つ",
        s="p189 の一覧　1〜2",
        fig=("absent", dict(
            mode="ledger", lead="大きく寄与しなかったもの（p189）",
            items=[dict(t="87 Park の工事の振動", d="大きく寄与せず", ok=False, c=J.LINE),
                   dict(t="基礎の破壊・陥没・不同沈下", d="大きく寄与せず", ok=False, c=J.LINE)],
            note="原文は p189 の1〜2項目")),
    ),

    # ── c724 5項目（後半）─────────────────
    "c724": dict(
        t="ハリケーン、衝撃、屋上の工事",
        s="p189 の一覧　3〜5",
        fig=("absent", dict(
            mode="ledger", lead="大きく寄与しなかったもの（p189・続き）",
            items=[dict(t="ハリケーンと高潮", d="大きく寄与せず", ok=False, c=J.LINE),
                   dict(t="衝撃荷重", d="大きく寄与せず", ok=False, c=J.LINE),
                   dict(t="屋上の改修・アンカー工事（進行中）", d="大きく寄与せず", ok=False, c=J.LINE)],
            note="原文は p189 の3〜5項目")),
    ),

    # ── c725 沈下について ─────────────────
    "c725": dict(
        t="地盤の沈下も、この一覧の中",
        s="沈下について（p189）",
        fig=("panel", dict(
            lead="p189 の2番目の項目",
            blocks=[dict(k="陥没", t="sinkholes", c=J.LINE),
                    dict(k="不同沈下", t="differential settlement", c=J.LINE)],
            cols=2, note="どちらも「大きくは効いていない」側に入っている")),
    ),

    # ── c726 外から加えられたものではない ──────────
    "c726": dict(
        t="限界を越えたのは、建物自身のつくり",
        s="現場の引き（NIST ドローン）",
        photo=ss.fb("c726"), bias=0.5, side="right", ann_y=360,
        ann=[dict(t="外からの力", d="原因ではない（p189）", dc=J.LINE, ds=32)],
    ),
}
