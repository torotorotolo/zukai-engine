# -*- coding: utf-8 -*-
"""第5章。**空**。

🔴 2026-09-13（7本目②）: **6本目キー橋（216カット）の中身を捨てて空にした。**
   捨てた中身は git の `eb322ca` にある（`git show eb322ca:tools/cuts/c5.py`）。

⚠️ **カットIDは題材をまたいでぶつかる。**前の回の SPEC が残ったままだと
   エラーも出ず、机上検査も全通してしまう（`cuts/__init__.py` の docstring）。
   → ②の入口で必ず空にする。検算＝`python -c "import cuts; print(len(cuts.SPEC))"` が **0**。

7本目（9.11・35分・202カット）の画は、**④の台本が承認されてから**ここに書く。
"""

SPEC = {}
